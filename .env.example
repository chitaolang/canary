# Sui Network Configuration
SUI_NETWORK=mainnet
# Options: mainnet, testnet, devnet, localnet

# Sui RPC Endpoint (Optional; if not set, the default network endpoint will be used.)
# SUI_RPC_URL=https://fullnode.mainnet.sui.io:443

# Registry Object ID
REGISTRY_OBJECT_ID=0x...

# Claude API Configuration
CLAUDE_API_KEY=sk-ant-...
CLAUDE_API_URL=https://api.anthropic.com/v1/messages
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Walrus Configuration
WALRUS_ENDPOINT=https://...
WALRUS_API_KEY=...
WALRUS_EPOCH_COUNT=10

# Seal SDK Configuration (If additional configuration is required)
# SEAL_CONFIG_PATH=/app/config/seal.toml

# Derived Object Prefix (Used to generate derived object addresses)
DERIVED_OBJECT_PREFIX=canary

# Application Settings
RUST_LOG=info
WORKSPACE_DIR=/app/workspace
MOVE_DECOMPILER_PATH=/app/move-decompiler

# Task Schedule (If additional configuration is required)
# TASK_INTERVAL_SECONDS=3600
